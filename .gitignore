models/
build/
# Any other build tree: build-tests/, build-engine/, build-tracy/ and the like.
# Nothing tracked begins with "build", so this cannot hide a real file.
build*/
latest/
out/
.vs/
.vscode/
.idea/
*.user
*.suo
CMakeUserPresets.json
compile_commands.json
Thumbs.db
spikes/**/bin/
spikes/**/build/
*.wav

# spike evidence too large or vendored for git
spikes/**/build-*/
spikes/**/out/
spikes/tts_src/zonos2.cpp/
spikes/tts_src/zonos2.cpp-b/
spikes/tts_cpu/voicevox/voicevox_core/
spikes/tts_cpu/voicevox/download-windows-x64.exe
spikes/stt/testdata/
*.dll
*.exe
*.lib
*.obj
spikes/**/*.log

# Agent worktrees: isolated checkouts, never part of the repo
.claude/worktrees/

# Local build-and-launch helper, per machine
run.bat

# Agent instructions for this checkout; kept locally, never committed
CLAUDE.md
